const upperCase = string => string.toUpperCase();

const firstLetterOfTheWord = string => string[0];

const concatenateString = (char1, char2) => char1 + char2;

module.exports = { upperCase, firstLetterOfTheWord, concatenateString };